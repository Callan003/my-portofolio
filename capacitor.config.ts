import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.mcstroescu.portofolio',
  appName: 'my-portofolio',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
